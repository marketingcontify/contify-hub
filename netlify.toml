[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"

# Pretty URLs: /company/google/events.html → /company/google/events/
[build.processing.html]
  pretty_urls = true
